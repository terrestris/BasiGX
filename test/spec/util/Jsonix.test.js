Ext.Loader.syncRequire(['BasiGX.util.Jsonix']);

describe('BasiGX.util.Jsonix', function() {
    describe('Basics', function() {
        it('is defined', function() {
            expect(BasiGX.util.Jsonix).to.not.be(undefined);
        });
    });
});
